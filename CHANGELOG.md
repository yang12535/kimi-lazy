# Changelog

## 0.1.4 — 2026-09-12

- 修复 0.1.3 把 HistoryWindow 的存在误判为原生开窗已启用，导致 0.42.0 默认页面全量挂载、四项设置失效。现在只让实际开启的原生窗口接管；未开启时适配器继续管理消息、内容块和工具条目。
- 修复折叠体被上游移除或停用后保留旧统计的问题；原生模式明确显示附加折叠区回收数，隐藏不适用的设置，避免被误解为全部消息都休眠。
- 加强 HistoryWindow 上游契约硬检查，拒绝缺少属性、启用分支或关闭插槽结构的新构建；修正出错回退后的面板提示，并明确设备测试的历史范围。
- 测试夹具改用真实的 items/itemKey 和编译后作用域插槽契约，覆盖原生开关两条路径及数量设置、滚动恢复、闲置回收和停用还原。

## 0.1.3 — 2026-09-12

- 修复：kimi-code CLI 0.42.0 前端把消息列表移入新的 HistoryWindow 组件，适配器结构自检报「消息列表结构与适配版本不同」并退回原生界面。当时误判上游已对消息列表、每条消息的内容块和工具组条目全部启用原生开窗（此判断在 0.1.4 修正），适配器不再重复插手这些列表，只做折叠工具组 / 思考块的回收与兼容；该版本下状态计数显示内容块（块）而非消息（条）。
- 测试：夹具新增 HistoryWindow 模式（?hw=1）与 7 项结构检查，纳入 CI 浏览器回归。

## 0.1.2 — 2026-09-09

- 适配：加入 kimi-code CLI 0.42.0 前端 `/assets/index-HU0LCM-X.js`；白名单覆盖 0.33.0–0.42.0 的 13 个构建，四个入口的一致性由 CI 检查。
- CI：修复 watch-upstream 正文生成、旧分支恢复和状态提交流程，并加入全量浏览器回归。

- 新增：悬浮面板可从固定右上角拖动到屏幕任意边缘，鼠标与触摸均可（Pointer Events）；拖动超过 6px 才判定为移动，普通点击开合面板不受影响。
- 面板位置按网站记住（用户脚本存 localStorage，扩展存 chrome.storage.local），以视口比例保存，窗口缩放或手机旋转后自动钳回可视范围内。
- 设置页按实际可用空间选择展开方向，钳制宽高与位置，保留手机安全区。

## 0.1.1 — 2026-09-06

- 修复：Kimi Web 前端 0.37.2（构建 `/assets/index-BkUUBejk.js`）下脚本不启用。该构建与 0.39.1 组件契约一致（Vue 3.5.39，ChatPane/ActivityRun/TurnFold/ThinkingBlock 及各 class、事件名逐一核对），加入已知构建白名单；未知构建仍不启用。
- 铺底：白名单覆盖 kimi-code CLI 0.33.0–0.41.0 全部 12 个前端构建（npm 全历史契约核对，kimi-lazy-watch 流水线产出）；≤0.32.0 的老构建无 ActivityRun/TurnFold 架构，不适用。

## 0.1.0 — 2026-09-06

首次公开发布 Chrome 扩展与 Via / 用户脚本版本。

- 最近消息窗口、工具内容按需恢复与十分钟闲置回收。
- 原生历史读取、流式更新保护、停用还原。
- 用户脚本自动识别页面，不限域名；手机设置面板。
- CRX3、扩展 ZIP、单文件用户脚本、更新元数据和校验和。
- Kimi Web 0.39.1 构建适配与未知构建保护。

已知限制：Via 首次注入可能晚于原生首屏渲染；原始会话缓存仍由 Kimi 保留。
