# 验证范围

- Node：8 项测试通过，涵盖窗口租约、可见内容保护、消息更新、会话清理、普通页面/未知构建退出和重复框架保护。
- 桌面 Chrome：合成会话 13 项功能回归通过；保存配置后首次挂载遵循设置，重复注入不重复注册，存储不可用时有明确提示；320 × 640 视口面板未溢出，按钮最小高度 44px。
- Waydroid：Android 13、Via 7.3.3、WebView 146.0.7680.153。通过 Via 自己的脚本安装入口安装后，13 项功能回归通过，结果见 [via-results.json](../tests/via-results.json)。
- 真实 Kimi 会话：面板显示 20 条已渲染、39 条休眠。关闭再启用可还原；没有向真实会话发送提示词。

Via 首轮快速页面测试晚注入，先挂载了 1500 个组件，随后收回旧组件。第二轮成功在首次渲染前接入，只挂载 200 个组件。因此不保证每次消除初次加载峰值。

十分钟回收使用模拟时间验证；流式更新、全历史读取在合成会话中验证。没有测量帧率或 JS 堆内存，也没有把 DOM 降幅当作总内存降幅。Chrome 扩展的原始适配核心在真实页面通过临时注入验证，CRX 安装结果应与打包/签名验证分开看待。
